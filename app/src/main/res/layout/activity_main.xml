<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="20dp"
    android:background="#121212">

    <TextView
        android:id="@+id/tvStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Status: Disconnected"
        android:textColor="#FF5252"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginBottom="16dp"/>

    <ImageView
        android:id="@+id/ivNecklaceFeed"
        android:layout_width="320dp"
        android:layout_height="240dp"
        android:background="#2A2A2A"
        android:scaleType="fitCenter"/>

    <!-- Discovery controls: shown before a connection is established -->
    <LinearLayout
        android:id="@+id/layoutDiscovery"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="16dp">

        <Button
            android:id="@+id/btnScan"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Scan for Pi"
            android:backgroundTint="#2979FF"/>

        <Button
            android:id="@+id/btnConnect"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="Connect"
            android:enabled="false"
            android:backgroundTint="#00E676"/>
    </LinearLayout>

    <EditText
        android:id="@+id/etSpeechInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Type speech response for Pi speaker..."
        android:textColorHint="#888888"
        android:textColor="#FFFFFF"
        android:layout_marginTop="20dp"
        android:padding="12dp"
        android:background="#2A2A2A"/>

    <Button
        android:id="@+id/btnSendSpeech"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Send Audio to Pi Speaker"
        android:layout_marginTop="10dp"
        android:backgroundTint="#00E676"/>
</LinearLayout>
